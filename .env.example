# Copy to .env.local to override locally.
#
# Leave unset in dev — the analytics module no-ops when the ID is missing.
# When you do want to test GA4 wiring locally, set this and run `npm run build
# && npm run preview` (the dev server still no-ops because IS_PROD is false).
VITE_GA_MEASUREMENT_ID=
